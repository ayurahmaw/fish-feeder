# MQTT
MQTT_URL=mqtt://localhost:1883
MQTT_TOPIC_PREFIX=feeder

# MySQL (user aplikasi, bukan root — lihat database/app_user.sql)
DB_HOST=localhost
DB_PORT=3306
DB_USER=feeder_app
DB_PASSWORD=feeder_dev_123
DB_NAME=feeder_db

# Perintah tanpa ack > 30 detik -> timeout
COMMAND_TIMEOUT_SEC=30
SWEEP_INTERVAL_MS=10000
